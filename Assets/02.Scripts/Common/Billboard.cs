using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace lsy
{
    public class Billboard : MonoBehaviour
    {
        private void Start()
        {
            //BillboardUIController billboardController = Managers.Instance.UIManager.BillboardUIController;
            //billboardController.AddTarget(gameObject, new Vector3(0f, 1.5f, 0f));
            //transform.parent = billboardController.transform;
        }
    }
}
